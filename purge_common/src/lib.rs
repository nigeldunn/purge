pub fn hello_world(name: String) -> String {
    format!("Hello, {}!", name)
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
